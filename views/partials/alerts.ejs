<% if (alerts.error) { %>
  <% alerts.error[0].split(',').forEach(msg => { %>
    <div style="color: red"><%= msg %></div>
  <% }) %>
<% } %>

<% if (alerts.success) { %>
  <% alerts.success[0].split(',').forEach(msg => { %>
    <div style="color: green"><%= msg %></div>
  <% }) %>
<% } %>